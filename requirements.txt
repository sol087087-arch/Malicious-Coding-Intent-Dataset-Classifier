# Core
pyyaml>=6.0
pandas>=2.0
pyarrow>=14.0
datasets>=2.0
huggingface_hub>=0.23

# Classifier train + inference (BGE-m3 + sklearn heads)
numpy>=1.24
scikit-learn>=1.3
joblib>=1.3
sentence-transformers>=3.0
# Install torch separately for your platform/CUDA, e.g.:
#   pip install torch --index-url https://download.pytorch.org/whl/cu130
torch>=2.0

# SSL for HF hub (train_classifier.py)
certifi>=2023.0

# Optional: translate_lexicon.py --backend nllb
# transformers>=4.40
# sentencepiece>=0.2
